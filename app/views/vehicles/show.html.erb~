<p id="notice"><%= notice %></p>
<div style="float:left; width: 40%">
<p>
  <b>Make and Model:</b>
  <%= @vehicle.make_and_model %>
</p>

<p>
  <b>Color:</b>
  <%= @vehicle.color %>
</p>

<p>
  <b>Year:</b>
  <%= @vehicle.year %>
</p>

<p>
  <b>License plate number:</b>
  <%= @vehicle.license_plate_number %>
</p>
</div>
<div>

<%= form_for(@mileage) do |f| %>
    <%= f.hidden_field :vehicle_id, :value => vehicle.id %>

  <div class="field">
    <%= f.label :filled_tank_on %>
    <%= f.date_select :filled_tank_on, :order => [:month, :day, :year] %>
  </div>

  <div class="field">
    <%= f.label :miles %>
    <%= f.text_field :miles %>
  </div>

  <div class="field">
    <%= f.label :gallons %>
    <%= f.text_field :gallons %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
  
</div>
<div style="clear:both;">
<h1>Mileages</h1>

<table>
  <tr>
    <td>Date Filled</td>
    <td>Miles</td>
    <td>Gallons</td>
  </tr>
  <% @mileages.each do |mileage| %>
  <tr>
    <td><%= mileage.filled_tank_on %></td>
    <td><%= mileage.miles %></td>
    <td><%= mileage.gallons %></td>
  </tr>
  <% end %>
</table>

<%= link_to 'Edit', edit_vehicle_path(@vehicle) %> |
<%= link_to 'List', vehicles_path %>


